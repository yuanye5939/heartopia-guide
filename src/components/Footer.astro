---
import { getLangFromPath, getUIStrings } from "../i18n/utils";
import { LANGUAGES } from "../i18n/ui";

const lang = getLangFromPath(Astro.url?.pathname || "/");
const t = getUIStrings(lang);
---

<footer class="relative mt-20 pt-16 pb-12">
  <!-- Wave divider -->
  <div class="absolute top-0 left-0 w-full overflow-hidden leading-none rotate-180 translate-y-[-99%]">
    <svg class="relative block w-full h-10" viewBox="0 0 1200 40" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M0 20c100-16 300-24 400-12 100 12 300 24 400 12 100-12 300-24 400-12v32H0z" fill="rgb(17 24 39)" />
    </svg>
  </div>
  <div class="max-w-6xl mx-auto px-4">
    <div class="flex flex-col md:flex-row justify-between items-center gap-4">
      <div class="text-center md:text-left">
        <p class="text-gray-400 text-sm">
          &copy; {new Date().getFullYear()} Heartopia Guide. {t.footer.disclaimer}
        </p>
      </div>
      <div class="flex gap-6 text-sm text-gray-500">
        <a href={lang === "en" ? "/" : `/${lang}/`} class="hover:text-gray-300 transition-colors">Home</a>
        <span class="text-gray-700">|</span>
        <span>{t.footer.languages.replace("{count}", String(LANGUAGES.length))}</span>
      </div>
    </div>
  </div>
</footer>
